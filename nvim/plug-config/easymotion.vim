" Mappings are defined in the nvim/keys/which-key.vim

" This doesn't work with which-key <- Actually there maybe a way to get it to
" work based on the which-key readme about ignoring some mappings
" map <leader>e <Plug>(easymotion-prefix)

let g:EasyMotion_smartcase = 1

" Hints shown with capitals, but accepts lower case typed in
" let g:EasyMotion_use_upper = 1
let g:EasyMotion_keys = 'abcdefghijklmnopqrstuvwxyz:'

let g:EasyMotion_move_highlight = 0

let g:EasyMotion_enter_jump_first = 1

